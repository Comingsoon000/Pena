export enum VariantButton {
  TEXT = 'text',
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
}

export enum ColorButton {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export enum TypeButton {
  BUTTON = 'button',
  SUBMIT = 'submit',
}
